mixin modal(mods)
  .modal(class= mods && "modal--preload " + mods)
    .modal__wrapper
      .modal__overlay
      .modal__content
        block
        button.modal__close-btn.btn-reset(type="button", aria-label="Закрыть попап")
          +icon("icon-close", 15, 15)
